---
- name: OpenShift local storage operator's local volume set creation playbook
  hosts: localhost
  roles:
    - role: openshift_lso_volume_set
      vars:
        file_dest_dir: /tmp/ODF_yaml_files